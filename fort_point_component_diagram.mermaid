```mermaid
graph TB
    %% ── Clients ──────────────────────────────────────────────
    WebApp["**«component»**\nWeb App\n─────────────\nReact 18 + Vite\nTailwind CSS\nDeployed on Vercel"]
    MobApp["**«component»**\nMobile App\n─────────────\nKotlin · Jetpack Compose\nRetrofit Client"]

    %% ── Core Backend ─────────────────────────────────────────
    Backend["**«component»**\nFort Point Backend API\n─────────────────────────────\nJava 17 · Spring Boot 3\nSpring Security · Spring Data JPA\nHosted on Render"]

    %% ── Internal Modules ─────────────────────────────────────
    Auth["**«component»**\nAuth Module\n──────────────\nJWT · bcrypt\nRole-Based Access Control\n(Admin / Agent / User)"]

    %% ── Data Layer ───────────────────────────────────────────
    DB["**«component»**\nPostgreSQL 14+\n──────────────\nSupabase Hosted\nJPA / Hibernate\nSQL Connection"]
    FileStore["**«component»**\nSupabase Storage\n──────────────\nPDF / Image Files\nREST API Access"]

    %% ── External Services ────────────────────────────────────
    SMTP["**«component»**\nSMTP Service\n──────────────\nEmail Notifications\nApplication Receipts"]

    %% ── Connections ──────────────────────────────────────────
    WebApp  -->|"REST API (HTTPS + JSON)\nJWT Authentication"| Backend
    MobApp  -->|"REST API (Retrofit Client)\nHTTPS + JWT"| Backend

    Backend -->|"JWT Issuance\nRole Verification"| Auth
    Backend -->|"JPA / Hibernate\nSQL Connection"| DB
    Backend -->|"Supabase Storage REST API\nPDF Upload / Retrieval"| FileStore
    Backend -->|"SMTP Email Service\nNotifications & Receipts"| SMTP

    %% ── Styling ──────────────────────────────────────────────
    classDef client   fill:#ffffff,stroke:#1a1a1a,stroke-width:2px,color:#1a1a1a
    classDef core     fill:#1a1a1a,stroke:#1a1a1a,stroke-width:2px,color:#ffffff
    classDef data     fill:#f0eeea,stroke:#1a1a1a,stroke-width:1.5px,color:#1a1a1a
    classDef external fill:#ffffff,stroke:#777,stroke-width:1.5px,stroke-dasharray:4 3,color:#333

    class WebApp,MobApp client
    class Backend core
    class Auth,DB,FileStore data
    class SMTP external
```
